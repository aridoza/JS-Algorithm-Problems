// Angry Professor\\
/*
Q:

class canceled if less than K students present when class starts
N students total
T = number of test cases
each test case has N and K (cancellation threshold)
negative = arrived early; 0 = still on time; > 0 is minutes late
YES = class is cancelled (< K students arrived on time or early)
NO = class not cancelled (at least K students arrived on time or early)

*/

angryProf = (t,nArr,kArr,times) => {
  // see hackerrank code for this example
}
